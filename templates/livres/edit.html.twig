{% extends 'base.html.twig' %}

{% block title %}Modifier un livre{% endblock %}

{% block body %}
<h1>Modifier le livre : {{ livre.titre }}</h1>

{{ form_start(f) }}
    {{ form_widget(f) }}

{{ form_end(f) }}
{% endblock %}
